# ml-core-concepts
A collection of Jupyter Notebooks mapping out every major machine learning model. Focuses strictly on model behavior, code implementation, and preprocessing rules (like feature scaling and threshold tuning) without application bloat.Models Covered:Naive Bayes, KNN, SVMLinear &amp; Logistic RegressionTrees &amp; EnsemblesPCA &amp; Clustering
